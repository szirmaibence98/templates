name: GitVersion

on:
  workflow_call:
    outputs:
      version:
        description: "The calculated version"
        value: ${{ jobs.gitversion.outputs.version }}

      release:
        description: 'Skip workflow if set to true for releases'
        required: false
        type: boolean
        

jobs:
  gitversion:
    name: 'Determine Version'
    runs-on: ubuntu-latest
    if: ${{ inputs.release == false }} 
    
    outputs:
      version: ${{ steps.set_tag.outputs.tag }}
      
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4.1.1
        with:
          fetch-depth: 0
          
      - name: Install GitVersion
        uses: gittools/actions/gitversion/setup@v0
        with:
          versionSpec: '5.x'
          preferLatestVersion: true
          
      - name: Run GitVersion
        id: gitversion
        uses: gittools/actions/gitversion/execute@v0
        with:
          useConfigFile: true
          configFilePath: 'GitVersion.yml'

      - name: Set Docker tag with Branch Name
        id: set_tag
        run: |
          # Read the version from GitVersion output
          FULL_SEM_VER="${{ steps.gitversion.outputs.fullSemVer }}"
          MAJOR_MINOR_PATCH="${{ steps.gitversion.outputs.majorMinorPatch }}"
          
          # Get the branch name, assuming GitHub Actions syntax. Adjust for your CI/CD system.
          BRANCH_NAME="${GITHUB_REF#refs/heads/}"
          
          # Check if the branch is a feature branch
          if [[ "$BRANCH_NAME" == feature/* ]]; then
              # For feature branches, use the fullSemVer
              TAG="$FULL_SEM_VER"
          else
              # For non-feature branches, use majorMinorPatch
              TAG="$MAJOR_MINOR_PATCH"
          fi
          
          echo "Tag: $TAG"
          echo "::set-output name=tag::${TAG}"


        
      - name: Save version to file
        run: echo "${{ steps.set_tag.outputs.tag }}" > /tmp/version.txt

      - name: Upload version artifact
        uses: actions/upload-artifact@v4.3.1
        with:
          name: version
          path: /tmp/version.txt

      - name: Calculate Next Version with GitVersion
        id: gitversion # Use GitVersion to calculate version
        run: |
          VERSION=$(gitversion /showvariable FullSemVer) # Assuming gitversion is available and configured
          echo "::set-output name=version::$VERSION"

      
    
      - name: Create Tag (if RELEASE is true and branch is main)
        if: github.ref == 'refs/heads/main' && github.event.inputs.release == 'true'
        run: |
          git config --global user.email "you@example.com"
          git config --global user.name "Your Name"
          VERSION="${{ steps.set_tag.outputs.tag }}"
          git tag -a $VERSION -m "Release version $VERSION"
          git push origin $VERSION



      
  
#      - name: Tag Release
#        if: env.RELEASE == 'true'
#        run: |
#          git tag -a v${{ steps.gitversion.outputs.majorMinorPatch }} -m "Release ${{ steps.gitversion.outputs.majorMinorPatch }}"
#          git push origin v${{ steps.gitversion.outputs.majorMinorPatch }}
#        env:
#          RELEASE: ${{ github.event.inputs.RELEASE }}
